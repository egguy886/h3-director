interface:
  display_name: "H3 短剧总导演"
  short_description: "把剧本拆成连续、含原文对白且可串行接力的 H3 短剧片段"
  default_prompt: "Use $h3-drama-director to turn this episode script into a validated H3 production package."

