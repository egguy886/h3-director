interface:
  display_name: "H3 Director"
  short_description: "Use an eight-step director workflow to compile MiniMax H3 prompts"
  default_prompt: "Use $h3-director: complete the eight-step asset-first director contract, map the references, and compile a production-ready MiniMax H3 prompt."

